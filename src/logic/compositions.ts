type Player = {
  name: string
  singles: number
  doubles: number
}

type SinglesComposition = {
  total: number
  players: Player[]
}

export function findSingleCompositions(_players: Player[], _limit: number): SinglesComposition[] {
  return []
}
